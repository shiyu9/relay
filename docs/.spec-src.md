題名: relay 仕様書
対象: Claude Code の会話ログから引き継ぎを書き起こし、次のセッションへ渡すプラグイン
生成元: tests/e2e/（95 件）・tests/test_battery.py（65 件）・docs/adr.md（62 件）

## 1 文書情報
@def
文書名: relay 仕様書
版: v0.7.0 時点
扱い: 生成物。手で編集しない。仕様が変わったら E2E と ADR から再生成する
仕様の正: `tests/e2e/test_recording.py` と `tests/test_battery.py`

## 2 用語定義
@def
transcript: Claude Code が 1 セッションにつき 1 本書く会話ログ。`~/.claude/projects/<プロジェクト>/<セッションID>.jsonl`
セッションID: transcript のファイル名にもなる UUID。日記の見出しには先頭 8 文字だけを載せる
日記: セッション 1 本につき 1 エントリの時系列記録。プロジェクト直下の `diary/YYYY-MM-DD.md`
ナレッジ: 寿命の長い知見。`knowledge/pitfalls.md`（技術的な罠）と `knowledge/workflow.md`（進め方の学び）
現在地: いまどこにいて次に何をするかを書いた 1 枚。`knowledge/current.md`。毎回上書きされる
見送り: 採らなかった案と、それを再検討する条件。`knowledge/decided.md`
タスク: 完了するまで残る未完の作業。プロジェクト直下の `tasks.md` の `- [ ]` 行
記録係: 記録を実際に書くサブエージェント。既定は `claude-haiku-4-5`
指示書: 記録係に渡す長い指示の本文。`~/.claude/relay/jobs/` に置かれ、セッション本体の文脈には入らない
受け皿: 記録係が書き出した区切り付きテキストの置き場。`~/.claude/relay/inbox/`
epoch: そのプロジェクトに relay を導入した時刻。これより前に終わった transcript は記録の対象にしない
終了印: セッションが閉じた事実を表す空ファイル。`~/.claude/relay/ended/`
空セッションの印: 記録係が「書くことが無い」と判断したセッションに残す 1 行。日記に置かれ、注入には出ない

## 3 システム概要

### 3.1 概要
@p relay は、Claude Code のセッションが終わるたびに会話ログを読んで要約し、**次のセッションの開始時にその要約を注入する**プラグインである。人が引き継ぎを書かなくても、次のセッションが前回の続きから始められる状態を保つ。
@p **できること。** 会話ログから日記・ナレッジ・現在地・見送りを書き起こす。完了したタスクを `tasks.md` から消す。起動時にそれらを注入する。記録はセッション自身のサブエージェントが行うため、ヘッドレスの `claude` CLI を必要としない。
@p **やらないこと。** セッションの途中で口を出さない。タスクを追加しない（消すだけ）。会話ログを書き換えない。プロジェクトの構成を仮定しない。
@p **書き込みはすべて Python が決定的に行う。** 記録係は区切り付きのテキストを一時ファイルへ出すだけで、日記もナレッジも直接触らない。安価なモデルに追記を任せて既存エントリを消失させた事故が実際にあったため（D-02）。
@p 適用の範囲は第 11 章、決定の理由は第 12 章に分けて書く。

### 3.2 適用場面

@purpose P-01 次のセッションを、前回までの記録から始める
@p 起動時に引き継ぎを注入し、記録し残したセッションがあればその手順も渡す。P-02 を忘れた場合の受け皿がここにあたる。
@flow P-01
端子 セッションが起動する
工程 引き継ぎを注入する | F-01
工程 記録すべきセッションを洗い出す | F-03
工程 指示書を書き出し、実行手順を注入する | F-04
工程 記録係が transcript を読み、要約を書き出す
工程 日記にエントリを書き入れる | F-05 F-06
工程 ナレッジに項目を足す | F-07
工程 現在地と見送りを書き換える | F-08
工程 完了したタスクを消す | F-09
工程 条件を満たしていればナレッジを統合する | F-10
端子 前回の続きから作業を始められる
@caption 起動時の記録は 1 回につき 3 本まで。残りは次の起動に回る（N-08）。
@purpose P-02 セッションを閉じる前に、そのセッション自身を記録に残す
@p 利用者が「記録して」「店じまいして」と頼むと `/relay:record` が走る。**起動時の記録と違い、いま動いているセッション自身も対象に入る**——通常の判定は「終了印がある、または 30 分以上更新なし」なので、走行中のセッションは決して対象にならないため。自分を見分けるために、スキルは毎回新しいトークンを作ってコマンドに載せる。ツール呼び出しは実行される前に transcript へ書かれるので、スクリプトは自分が書き込まれているファイルを名指しできる。
@flow P-02
端子 記録を頼まれる
工程 このセッションの transcript を特定する | F-02
工程 記録すべきセッションを洗い出す | F-03
工程 指示書を書き出し、実行手順を印字する | F-04
工程 記録係が transcript を読み、要約を書き出す
工程 日記にエントリを書き入れる | F-05 F-06
工程 ナレッジに項目を足す | F-07
工程 現在地と見送りを書き換える | F-08
工程 完了したタスクを消す | F-09
工程 条件を満たしていればナレッジを統合する | F-10
端子 記録が残る
@caption 特定できたセッションだけが終了判定を免除される。ほかの transcript は通常の判定のまま扱われるので、別のウィンドウで進行中のセッションが途中で要約されることはない（N-03）。

@purpose P-03 セッションが終わった事実を残す
@p 「静かなだけ」と「終わった」を次の起動が区別できるようにする。これが無いと 30 分の無更新を待つしかなく、閉じてすぐ開き直した場合に直前のセッションが引き継がれない。
@flow P-03
端子 セッションが閉じる
工程 終了印を 1 つ置く | F-11
端子 次の起動がこのセッションを終了扱いにできる
@caption 印が書けなくても 30 分の無更新で拾えるため、これは最適化であって依存ではない（D-06）。

## 4 システム構成

### 4.1 構成要素
@def
起動フック: 新規起動と `/clear` のときに走り、引き継ぎと記録手順を標準出力へ出す
終了フック: セッションが閉じるときに走り、終了印を 1 つ置く
記録スキル: 利用者が明示的に頼んだときだけ走る `/relay:record`
検出: 日記と transcript を突き合わせ、記録すべきセッションを決める
ジョブ生成: 記録係への指示書を書き出し、それを呼ぶ手順を組み立てる
反映: 受け皿のテキストを読み、日記・ナレッジ・現在地・タスクへ決定的に書き込む
記録係: Claude Code のサブエージェント。transcript を読んで区切り付きテキストを書き出す
プロジェクトの記録: 日記・ナレッジ・現在地・見送り・タスク
作業領域: `~/.claude/relay/` 配下の epoch・終了印・指示書・受け皿・ログ

### 4.2 要素間の関係
@graph
@hub 検出 | 「まだ記録されていないのはどれか」を答える唯一の場所
起動フック -> 検出: 未記録セッションの問い合わせ
記録スキル -> 検出: 未記録セッションの問い合わせ（自分自身を加えて）
検出 -> プロジェクトの記録: 日記のエントリを読む
検出 -> 作業領域: epoch と終了印を読む
起動フック -> ジョブ生成: 手順の組み立て
記録スキル -> ジョブ生成: 手順の組み立て
ジョブ生成 -> 作業領域: 指示書の書き出し
記録係 -> 作業領域: 指示書を読み、要約を受け皿へ
反映 -> 作業領域: 受け皿の読み取りと消費
反映 -> プロジェクトの記録: 日記・ナレッジ・現在地・タスクの更新
終了フック -> 作業領域: 終了印の作成
起動フック -> プロジェクトの記録: 引き継ぎの読み取り
@caption 検出は起動フックと記録スキルの両方から同じ関数として呼ばれる。2 つの入口が「何が未記録か」で食い違うことはない（D-07）。

### 4.3 動作環境
@def
実行系: Python 3（標準ライブラリのみ。外部依存なし）
必須の外部コマンド: なし。ヘッドレスの `claude` CLI を必要としない
検証済みの OS: Windows 11。macOS・Linux 向けの記述はコードにあるが未検証（X-05）
記録係のモデル: 既定 `claude-haiku-4-5`。`RELAY_MODEL` で変更できる
配置: Claude Code のプラグインとして `~/.claude/plugins/` 配下に置かれる
設定: 環境変数 `RELAY_SCOPE` `RELAY_DISABLED` `RELAY_MODEL` `RELAY_MIN_USER_MSGS` `RELAY_EPOCH`

## 5 インターフェース仕様

### 5.1 外部との境界
@io
@self relay | Claude Code プラグイン
利用者 <-> 記録の依頼 / 引き継ぎと実行手順
@path 受信
内容: 記録を実行せよという指示
手段: スキルの呼び出し（`/relay:record`）と、コマンドラインのトークン
形式: `python <スキルの相対パス>/relay_record.py --cwd <プロジェクト> --self <トークン>`。トークンは毎回新しく作る
契機: 利用者が「記録して」「店じまいして」と明示的に頼んだとき
用途: このセッション自身を含めた記録を、起動を待たずに走らせる（F-02 F-03）
エラー: E-01 — このセッションを特定できないと伝え、自分自身だけを対象から外す
検証: test_the_session_running_it_is_recorded_too test_nothing_outstanding_says_so_plainly
@path 送信
内容: 前回までの引き継ぎと、記録の実行手順
手段: フックの標準出力（そのままセッションの文脈に入る）と、スキルの標準出力
形式: Markdown の見出し付きテキスト。手順は番号付きで、記録係へ渡すプロンプトを区切り行で挟んで載せる
契機: 起動時（引き継ぎと手順）と、スキルの実行時（手順のみ）
用途: 利用者と、そのセッションの Claude が前回の続きから作業を始める（F-01 F-04）
エラー: なし
検証: test_current_is_injected_last test_the_prompt_is_printed_whole_instead_of_described
Claude Code -> フックの起動とセッション情報
@path 受信
内容: 作業ディレクトリ、セッションID、起動の種別
手段: フックの標準入力に渡される JSON
形式: `{"cwd": …, "session_id": …, "source": "startup"}`。起動フックは `startup` と `clear` にだけ応じる
契機: セッションの起動時と終了時
用途: 対象プロジェクトを決め、終了印のファイル名にする（F-11 F-01）
エラー: E-08 — 読めない入力は「何もしない」に倒す
検証: test_it_writes_the_marker test_japanese_reaches_stdout_even_on_a_cp932_console
記録係 <-> 指示書 / 要約テキスト
@path 受信
内容: 区切りで囲まれた要約テキスト（日記・ナレッジ・現在地・見送り・完了タスク）
手段: 受け皿のファイル
形式: `===DIARY===` `===PITFALLS===` などの区切り行で節を分けたプレーンテキスト。書くことが無ければ `NOTHING_TO_RECORD` の 1 語
契機: 記録係が 1 本ぶんの仕事を終えたとき
用途: 反映が読み取り、日記とナレッジへ決定的に書き込む（F-05〜F-10）
エラー: E-06 — 区切りが 1 つも無ければ数えて捨てる
検証: test_append_and_replace_use_different_delimiters test_an_inbox_file_with_no_delimiters_is_counted_and_dropped
@path 送信
内容: 何を読み、何をどこへ書くかの指示
手段: 指示書のファイルと、それを指す短い呼び出し文
形式: 指示書は長い本文。呼び出し文は目的・番号付き手順・書き込む 1 ファイルを明示した形
契機: 記録すべきセッションが見つかったとき
用途: 記録係が transcript を読み、要約を受け皿へ書き出す（F-04）
エラー: E-07 — 呼び出しが分類器に拒否された場合、再送せず次の起動へ回す
検証: test_the_call_names_the_job_the_transcript_and_the_one_file_written test_a_refused_job_is_left_pending_instead_of_being_repeated
transcript -> 会話ログ
@path 受信
内容: 1 セッションぶんの発言・ツール呼び出し・タイムスタンプ
手段: `~/.claude/projects/<プロジェクト>/<セッションID>.jsonl` の読み取り
形式: 1 行 1 JSON。先頭行と末尾行にタイムスタンプが無いことがある
契機: 検出のたび（両端の 64KB だけ）と、記録係が読むとき（全文）
用途: 開始と終了の時刻を得て、記録の要否を決める（F-03）
エラー: E-09 — タイムスタンプが 1 つも無ければ、そのファイルは対象から外す
検証: test_a_span_is_found_even_though_the_last_line_has_no_timestamp test_a_large_transcript_is_never_read_end_to_end
プロジェクトの記録 <- 日記・ナレッジ・現在地・タスクの更新
@path 送信
内容: 日記のエントリ、ナレッジの項目、現在地と見送りの全文、消したタスク
手段: 一時ファイルへ書いてから置き換える原子的な書き込み
形式: 日記は `## S1 19:11-22:30 (124c52dd)` の見出しと 4 節。ナレッジは `- [YYYY-MM-DD] 命令形の 1 事実`
契機: 反映の実行時
用途: 次のセッションが読む引き継ぎになる（F-05〜F-10）
エラー: E-05 — 統合の材料が古ければ適用しない
検証: test_lines_outside_our_own_entries_are_never_altered test_a_prune_built_before_the_appends_is_refused
@caption transcript は読み取りのみで、relay が書き換えることはない（N-01）。

### 5.2 経路ごとの仕様
@paths

## 6 データ仕様

@store 日記 | プロジェクト直下の `diary/YYYY-MM-DD.md`
@p セッション 1 本につき 1 エントリ。ファイルは**そのセッション自身が始まった日付**で決まるので、遡って記録しても当日の日記に過去が流れ込まない。
@schema
見出し | 文字列 | - | - | 文字 | ○ | - | - | `## S<番号> <開始>-<終了>[+N d] (<セッションID の 8 文字>)`
エントリ番号 | 整数 | 1 | - | 件 | - | - | - | ファイル内での通し番号。Python が振る
セッションID断片 | 文字列 | 8 | 8 | 文字 | - | ○ | transcript | どの transcript の記録かを示す
日数 | 整数 | 0 | - | 日 | - | - | - | 日付をまたいだ場合の日数。0 なら見出しに出ない
done | 文字列 | - | - | 文字 | - | - | - | 実際に達成したこと
decisions | 文字列 | - | - | 文字 | - | - | - | 下した判断と理由。無ければ節ごと省かれる
mistakes | 文字列 | - | - | 文字 | - | - | - | 誤りと手戻り。無ければ節ごと省かれる
handoff | 文字列 | - | - | 文字 | - | - | - | 次のセッションに要ること。ファイルには残るが注入はされない

@store transcript | `~/.claude/projects/<プロジェクト>/<セッションID>.jsonl`
@p Claude Code が書く会話ログ。relay は読むだけで、書き換えも削除もしない。
@schema
セッションID | 文字列 | 36 | 36 | 文字 | ○ | - | - | ファイル名にもなる UUID
開始時刻 | 文字列 | - | - | ISO 8601 | - | - | - | 先頭側の 64KB から拾う最初のタイムスタンプ
終了時刻 | 文字列 | - | - | ISO 8601 | - | - | - | 末尾側の 64KB から拾う最後のタイムスタンプ
利用者の発言数 | 整数 | 0 | - | 件 | - | - | - | `tool_result` を含まない `type:"user"` 行の数

@store ナレッジ | プロジェクト直下の `knowledge/pitfalls.md` と `workflow.md`
@p 寿命の長い知見。追記と重複の統合はするが、**行数のために項目を削ることはしない**。
@schema
日付 | 文字列 | 10 | 10 | 文字 | - | - | - | `[YYYY-MM-DD]`。**Python が付ける**（モデルには書かせない）
本文 | 文字列 | - | - | 文字 | - | - | - | 命令形の 1 事実。1 項目 1 行

@store 現在地 | プロジェクト直下の `knowledge/current.md`
@p 「いまどこにいて、次に何をするか」を書く 1 枚。毎回まるごと書き換えられる。行数の上限は無い。
@schema
本文 | 文字列 | - | - | 文字 | - | - | - | 節が空で返ってきた場合は既存の内容を維持する

@store 見送り | プロジェクト直下の `knowledge/decided.md`
@p 採らなかった案と、それを再検討する条件。落とした項目はその日の日記に退避される。
@schema
日付 | 文字列 | 10 | 10 | 文字 | - | - | - | 見送りを決めた日
案 | 文字列 | - | - | 文字 | - | - | - | 何を見送ったか
理由 | 文字列 | - | - | 文字 | - | - | - | なぜ見送ったか
再開条件 | 文字列 | - | - | 文字 | - | - | - | 何が起きたら戻すか

@store タスク | プロジェクト直下の `tasks.md`
@p **relay は完了したものを消すだけで、項目は足さない。**書式は強制せず、読むのは `- [ ]` の行と、それに続くインデント行だけ。
@schema
未完の印 | 文字列 | 5 | 5 | 文字 | - | - | - | `- [ ]`。`- [x]` は relay の対象外
本文 | 文字列 | - | - | 文字 | ○ | - | - | 項目の 1 行目。消し込みはこの行の逐語一致だけで決まる
継続行 | 文字列 | - | - | 文字 | - | - | - | 1 行目より深いインデントの行。項目の一部として扱う
見出し | 文字列 | - | - | 文字 | - | - | - | 直前の Markdown 見出し。注入のときに 1 度だけ添える

@store 作業領域 | `~/.claude/relay/` 配下
@p プロジェクトごとに分かれた relay 自身の状態。プロジェクトの外にあるので、書き込みの許可を求められることがある。
@schema
epoch | 文字列 | - | - | ISO 8601 | - | - | - | 導入時刻。`epoch/<プロジェクト>` に 1 行。一度書かれたら動かない
終了印 | 文字列 | 36 | 36 | 文字 | - | ○ | transcript | `ended/<プロジェクト>/<セッションID>` の空ファイル
指示書 | 文字列 | - | - | 文字 | - | ○ | transcript | `jobs/<プロジェクト>/<セッションID>.md`。記録係だけが読む
受け皿 | 文字列 | - | - | 文字 | - | ○ | transcript | `inbox/<プロジェクト>/<セッションID>.txt`。反映が読んで消す
統合の指紋 | 文字列 | 64 | 64 | 文字 | - | - | - | ナレッジ 2 ファイルの sha256。統合の適用可否を決める
ログ | 文字列 | - | - | 文字 | - | - | - | `log.txt`。1 行 1 事象

## 8 機能仕様

@spec F-01 引き継ぎの注入 | 確定
概要: ナレッジ・日記・タスク・現在地を、この順に標準出力へ出す
実行契機: 新規起動と `/clear` のとき
前提条件: プロジェクトが対象範囲内で、無効化されていないこと
入力: ナレッジ 3 ファイル、日記の直近 5 エントリ、`tasks.md` の未完項目、現在地
処理: 日記からは handoff 節を落とす。現在地がそれを蒸留したものなので、両方入れると「何が残っているか」が二重になる
処理: `tasks.md` からは `- [ ]` の行と継続行、直近の見出しだけを取る
処理: 変わりにくいものから変わりやすいものへ並べる（現在地が最後）
出力: 標準出力へのテキスト。そのままセッションの文脈に入る
エラー: なし
検証: test_the_diary_is_cut_by_entry_count_not_by_day test_handoff_is_not_injected_but_stays_in_the_file test_current_is_injected_last test_the_unchecked_items_are_injected test_it_sits_between_the_diary_and_current

@spec F-02 このセッションの特定 | 確定
概要: コマンドラインに載せたトークンから、いま動いているセッションの transcript を突き止める
実行契機: 記録スキルの実行時（起動フックからは呼ばれない）
前提条件: トークンが指定されていること
入力: トークン、プロジェクトの transcript ディレクトリ
処理: 直近 5 分に更新された transcript だけを走査する
処理: トークンを含むものを集める
処理: ちょうど 1 本のときだけ、その ID を返す
出力: セッションID、または「特定できない」
エラー: E-01 — 0 本でも 2 本以上でも特定せず、自分自身だけを対象から外す
検証: test_one_recent_transcript_holding_the_token_is_us test_two_live_matches_refuse_rather_than_guess test_a_match_outside_the_window_does_not_count test_a_stale_copy_leaves_the_live_one_unambiguous test_an_empty_token_never_matches test_a_token_nobody_wrote_matches_nothing

@spec F-03 記録すべきセッションの洗い出し | 確定
概要: 日記と transcript を突き合わせ、まだ記録されていないセッションを新しい順に選ぶ
実行契機: 起動フックの実行時と、記録スキルの実行時
前提条件: なし
入力: プロジェクトの transcript 一式、日記のエントリ、epoch、終了印
処理: epoch より後に終わった transcript だけを候補にする
処理: 終了しているものだけを残す（終了印がある、または 30 分以上更新なし）。**特定できた自分自身はこの判定を免除する**
処理: 日記にエントリが無いもの、またはエントリより transcript が新しいものを残す
処理: 利用者の発言が既定 3 件に満たないものを落とす
処理: 新しい順に最大 3 本まで返す
出力: 記録すべきセッションの一覧（新しい順）
エラー: E-09 — タイムスタンプの読めない transcript は対象から外す
検証: test_a_running_window_is_not_summarised test_an_unchanged_transcript_is_not_offered_again test_three_at_a_time_and_the_pool_shrinks test_a_transcript_that_ended_before_the_line_is_never_offered test_the_session_running_it_is_recorded_too test_a_parallel_window_is_still_left_alone

@spec F-04 指示書の書き出しと手順の組み立て | 確定
概要: 記録係に渡す指示書をファイルへ書き、それを呼ぶ手順を印字する
実行契機: 記録すべきセッションが 1 本以上見つかったとき
前提条件: なし
入力: 記録すべきセッションの一覧、ナレッジの現在の内容、日記の直近エントリ、タスクの未完項目
処理: 記録の指示書を 1 セッションにつき 1 本書き出す
処理: 現在地の指示書と統合の指示書を書き出す
処理: 記録係へ渡すプロンプトを、目的・番号付き手順・書き込む 1 ファイルの形で組み立てる
処理: 手順の本体は起動フックとスキルで共有し、見出しの 1 文だけを差し替える
出力: 実行手順のテキスト（指示書の本文は含まない）
エラー: なし
検証: test_the_job_body_still_stays_out_of_the_notice test_the_apply_command_is_one_command_with_nothing_chained_to_it test_it_hands_over_the_same_jobs_startup_would test_the_steps_are_one_string test_only_the_framing_differs

@spec F-05 日記エントリの反映 | 確定
概要: 記録係が書いた要約を、そのセッションの日付のファイルへ書き入れる
実行契機: 反映の実行時、受け皿に日記の節があるとき
前提条件: そのセッションの transcript が実在すること
入力: 受け皿の `===DIARY===` 節
処理: 見出し（番号・時刻・日数・セッションID）を Python が組み立てる
処理: 同じセッションのエントリが既にあれば**丸ごと差し替える**。無ければ時刻順の位置に挿し込む
処理: 15 日以上あいて再開したセッションは、古いエントリを残して新しいエントリを足す
出力: 更新された日記ファイル
エラー: E-04 — 置き場所が決められなければ、そのテキストは捨てる
検証: test_the_entry_lands_in_the_files_dated_for_the_session test_a_grown_transcript_replaces_its_entry_whole test_a_multi_day_session_records_how_many_days_it_spanned test_a_gap_of_fifteen_days_gets_a_fresh_entry test_lines_outside_our_own_entries_are_never_altered

@spec F-06 空セッションの印 | 確定
概要: 記録係が「書くことが無い」と答えたセッションにも、日記に見出しと 1 行を残す
実行契機: 受け皿の内容が `NOTHING_TO_RECORD` だったとき
前提条件: そのセッションの transcript が実在すること
入力: 受け皿のテキスト
処理: 見出しと `（記録するものがなかったセッション）` の 1 行だけを日記へ書く
処理: この印を持つエントリは、注入と現在地の材料から外す
出力: 更新された日記ファイル
エラー: E-04 — 置き場所が決められなければ捨てる
検証: test_the_verdict_reaches_the_diary test_the_session_stops_being_a_candidate test_it_does_not_take_a_slot_in_the_injection test_a_real_entry_beside_it_still_shows test_growth_makes_it_a_candidate_again

@spec F-07 ナレッジへの追記 | 確定
概要: 新しく学んだ罠と進め方を、日付を付けてナレッジへ足す
実行契機: 反映の実行時、受け皿にナレッジの節があるとき
前提条件: なし
入力: 受け皿の `===PITFALLS===` `===WORKFLOW===` 節
処理: `[日付]` を Python が付ける。モデルが書いた日付は捨てる
処理: 日付はそのセッションが動いた日で、掘り起こした日ではない
処理: 既に同じ行があれば足さない
出力: 更新されたナレッジ 2 ファイル
エラー: なし
検証: test_an_item_written_without_a_date_still_gets_one test_a_date_the_model_chose_itself_is_replaced test_an_old_session_is_not_stamped_with_the_day_it_was_dug_up test_applying_the_same_bullets_twice_adds_them_once

@spec F-08 現在地と見送りの書き換え | 確定
概要: 現在地と見送りを、日記を材料にまるごと書き換える
実行契機: 日記が 1 件以上書かれた回の、その直後
前提条件: なし
入力: 受け皿の `===CURRENT===` `===DECIDED===` 節
処理: 節が空のときは既存の内容を維持する（「書き忘れ」と「本当に空」を区別できないため）
処理: 見送りから落ちた項目は、その日の日記へ退避する
出力: 更新された現在地と見送り
エラー: なし
検証: test_an_empty_current_section_keeps_the_page test_a_four_hundred_line_current_is_not_truncated test_a_dropped_deferral_is_written_to_todays_diary test_an_empty_overwrite_section_can_never_erase_the_page

@spec F-09 完了タスクの消し込み | 暫定
概要: 日記の done が完了したと言っているタスクを、`tasks.md` から消す
実行契機: 現在地の書き換えと同じ回
前提条件: `tasks.md` が実在すること
入力: 受け皿の `===TASKS_DONE===` 節、`tasks.md` の未完項目
処理: モデルが挙げた行を `tasks.md` の項目と**逐語で照合する**（言い換え・切り詰めでは一致しない）
処理: 一致した項目を継続行ごと消す
処理: 消した項目はその日の日記へ退避する
処理: 節が無くても空でも失敗にしない。何も消えないだけで、次の回にまた候補になる
出力: 更新された `tasks.md` と、日記の退避
エラー: なし
検証: ? test_a_verbatim_line_strikes_its_item_off test_a_paraphrase_removes_nothing test_a_truncated_line_removes_nothing test_striking_takes_the_continuation_lines_with_it test_a_missing_section_is_not_a_failure test_the_whole_item_lands_in_todays_diary test_a_recorded_session_can_strike_an_item_off

@spec F-10 ナレッジの統合 | 確定
概要: 重複した項目を 1 行にまとめ、矛盾を新しい日付の側へ寄せる
実行契機: ナレッジが 80 行を超えていて、かつ前回の統合から中身が変わっているとき
前提条件: 指示書を作ったときの本文と、現在のファイルが一致すること
入力: 受け皿の `===PRUNE_PITFALLS===` `===PRUNE_WORKFLOW===` `===MOVE===` 節
処理: **項目を削除しない。**重複を 1 行にまとめるだけで、行数のために削ることはしない
処理: 「別ファイルへ移してよさそうな項目」は理由付きで日記に書き出す。移すかどうかは人が決める
処理: 材料が古ければ適用せず、要約に `stale=1` を出す
出力: 統合されたナレッジ 2 ファイルと、日記の移設候補
エラー: E-05 — 材料が古い場合は適用しない
検証: test_the_prune_job_is_offered_only_past_the_limit test_a_prune_built_before_the_appends_is_refused test_the_job_is_never_told_to_hit_a_line_count test_move_candidates_go_to_the_diary_and_nothing_is_deleted test_the_merge_is_due_only_while_something_changed

@spec F-11 終了印の作成 | 確定
概要: セッションが閉じた事実を、空ファイル 1 つで残す
実行契機: セッションの終了時
前提条件: セッションIDが渡されていること
入力: 作業ディレクトリ、セッションID
処理: `~/.claude/relay/ended/<プロジェクト>/<セッションID>` を作る。ほかには何もしない
出力: 空ファイル 1 つ
エラー: なし。書けなくても 30 分の無更新で拾えるため、失敗しても黙って終わる
検証: test_it_writes_the_marker test_it_never_starts_a_process test_reopening_a_minute_later_still_records_the_previous_session

## 9 エラー仕様
@rows
E-01 | このセッションの transcript がトークンで一意に決まらない（0 本または 2 本以上） | `relay: このセッション自身は特定できませんでした` と伝え、別のトークンでの再実行を促す | 流れ不変 | test_two_matches_name_neither test_an_unresolvable_token_says_so_and_records_the_rest
E-02 | 記録すべきセッションが 1 本も無い | `relay: 記録するセッションはありません` | 流れ不変 | test_nothing_outstanding_says_so_plainly test_nothing_to_record_leaves_diary_at_zero
E-03 | プロジェクトが対象範囲外、または無効化されている | その旨を 1 行で伝え、何もしない | 流れ不変 | test_a_disabled_project_is_left_alone test_a_project_out_of_scope_is_left_alone test_relay_scope_still_keeps_other_projects_out
E-04 | 要約の置き場所が日記の中で決められない | 要約を捨て、要約行の `bad` に数える | 流れ不変 | test_a_diary_section_with_no_transcript_behind_it test_a_broken_id_heading_is_logged_and_its_session_skipped test_a_colliding_id_skips_that_session
E-05 | 統合の材料が古い（指示書を作った後にナレッジが変わった） | 統合を適用せず、要約行に `stale=1` を出す | 流れ不変 | test_a_prune_built_before_the_appends_is_refused
E-06 | 受け皿のテキストに区切りが 1 つも無い | 捨てて `bad` に数え、受け皿のファイルは消す | 流れ不変 | test_an_inbox_file_with_no_delimiters_is_counted_and_dropped test_a_consumed_inbox_file_is_gone
E-07 | 記録係の呼び出しが auto mode の分類器に拒否された | 同じ呼び出しを繰り返さず、次の起動へ回す。件数を 1 行で伝える | 流れ不変 | test_a_refused_job_is_left_pending_instead_of_being_repeated
E-08 | フックへの入力が読めない | 何もせずに終了コード 0 で終わる | 流れ不変 | test_it_exits_zero_even_when_everything_is_wrong
E-09 | transcript からタイムスタンプが 1 つも読めない | そのファイルを対象から外す | 流れ不変 | test_a_transcript_with_no_timestamps_has_no_span test_the_line_never_reaches_a_transcript_with_no_span
E-10 | epoch のファイルが日付として読めない | 線が無いものとして全件を候補に戻す | 流れ不変 | test_a_broken_line_records_everything_rather_than_nothing
E-11 | epoch のファイルが書けない | 線を適用せず、候補を全件見せる | 流れ不変 | test_an_unwritable_line_leaves_the_backlog_visible

## 10 非機能要件
@rows
N-01 | 安全性 | transcript を読み取りのみで扱い、書き込み・削除・属性変更を行わない | test_a_large_transcript_is_never_read_end_to_end
N-02 | 安全性 | relay 自身が書いた見出しの外にある行を、1 バイトも変えない | test_lines_outside_our_own_entries_are_never_altered test_a_hand_written_diary_survives_intact test_a_heading_without_an_id_is_never_matched
N-03 | 安全性 | 記録スキルが終了判定を免除するのは、トークンで特定できた 1 本だけとする | test_a_parallel_window_is_still_left_alone
N-04 | 安全性 | 上書き層の節が空で返っても、既存のページを消さない | test_an_empty_overwrite_section_can_never_erase_the_page test_an_empty_current_section_keeps_the_page
N-05 | 安全性 | 統合が適用されるのは、指示書を作ったときの本文と現在のファイルが一致するときだけとする | test_a_prune_built_before_the_appends_is_refused
N-06 | 冪等性 | 同じ受け皿を 2 回適用しても、日記のエントリもナレッジの項目も増えない | test_applying_twice_does_not_duplicate_the_entry test_applying_the_same_bullets_twice_adds_them_once test_detection_repeated_gives_the_same_answer
N-07 | 性能 | transcript の開始と終了は両端 64KB だけを読んで求め、全文を読まない | test_a_large_transcript_is_never_read_end_to_end
N-08 | 性能 | 1 回の起動で記録係に渡すのは最大 3 本とする | test_three_at_a_time_and_the_pool_shrinks
N-09 | 可搬性 | 記録の手順がヘッドレスの `claude` CLI に依存しない | test_the_procedure_never_depends_on_the_claude_cli
N-10 | 可搬性 | cp932 のコンソールでも日本語が標準出力へ出る | test_japanese_reaches_stdout_even_on_a_cp932_console
N-11 | 可搬性 | UTC のタイムスタンプを、TZ 環境変数が壊れていても現地時刻へ直す | test_utc_timestamps_become_local_times test_a_mangled_tz_does_not_shift_the_clock
N-12 | 頑健性 | 手で編集された日記、読めないファイル、壊れたディレクトリでも例外を投げない | test_a_hand_edited_diary_is_still_parsed test_an_unreadable_diary_file_does_not_raise test_a_broken_marker_directory_falls_back_to_the_idle_rule
N-13 | 出力契約 | 反映は要約を 1 行だけ出し、手順が読む項目をすべて含める | test_the_summary_is_one_line test_the_summary_has_every_field_the_procedure_reads

## 11 適用範囲外
@rows
X-01 | タスクの追加 | `tasks.md` に項目を足すのはセッション自身の仕事。relay は完了したものを消すだけ。追加は relay 無しで既に回っており、壊れているのは消し込みの側だった
X-02 | セッション途中への介入 | Stop フックを v0.7.0 で撤去した。relay が動くのはセッションの端（起動・終了）と、利用者が明示的に頼んだときだけ
X-03 | transcript の保持 | 会話ログを残すのは Claude Code の仕事（既定 30 日）。relay は独自の保持期間を持たず、消えた分は遡れない
X-04 | 導入前の履歴 | 導入した時点より前に終わった transcript は記録しない。遡りたい場合は epoch のファイルを人が書き換える
X-05 | macOS・Linux | コードに経路はあるが未検証。動作確認は Windows のみ
X-06 | `tasks.md` の書式の強制 | 見出しの立て方も優先度の付け方も各プロジェクトのまま。`- [ ]` を使っていない項目は読まれない
X-07 | 外部サービスとの連携 | 課題管理システムやチャットへの連携は持たない。書き先はプロジェクト内のファイルと `~/.claude/relay/` だけ

## 12 設計判断
@rows
D-01 | 日記とナレッジを分ける | 引き継ぎ資料と蒸留知見は寿命が違う。日記だけだと知見が日々に埋もれ、ナレッジだけだと時系列の引き継ぎが失われる
D-02 | ファイルへの書き込みをすべて Python が行う | 安価なモデルに「追記」を指示したところ、既存エントリを上書きして消した実バグがある。番号・時刻・挿入位置をコードへ移せば、喪失が構造的に起きない
D-03 | 記録をサブエージェントで行う | ヘッドレスの `claude` CLI が使えない環境では、それ以前の方式だと記録が一切残らなかった。タイミングの調整では解けず、手段を変える必要があった
D-04 | 1 transcript = 1 日記エントリ | 未記録かどうかが日記そのものから読めるので、二重記録を防ぐ外部の台帳が要らない。再開で伸びた transcript が同じセッションの要約を 2 つ生む欠陥も同時に消える
D-05 | 導入時に一度だけ線を引く | 線が動くと「昨日は内側、今日は外側」の取りこぼしが生まれる。一度きりの線には外側が無い。導入のたびに全履歴の要約代を払う問題も同時に解く
D-06 | 終了フックを印だけにする | ウィンドウが `claude` と同時に閉じる構成では、フックが完走前に殺される（ログ 1 行すら残らない例を観測）。空ファイル 1 つなら失敗しても 30 分の無更新で拾える
D-07 | 検出を 1 か所に持つ | 起動フックと記録スキルが同じ関数を呼ぶので、2 つの入口が「何が未記録か」で食い違わない
D-08 | 統合で項目を削除しない | 行数の上限に合わせて削ると、一次情報が失われる。80 行は「そこが育った」という合図として使い、移設の候補を挙げるだけにする
D-09 | Stop フックを撤去する | 毎ターン全 transcript を走査していた（実測 70〜250ms）。分類器に拒否されるとセッション内で解決できないまま 30 分おきに促しが出続けた。猶予と間隔の 2 つの定数に暗黙の順序依存もあった
D-10 | 記録を手動と自動の 2 段にする | 手動が基本、忘れたら次の起動が拾う。手動にしかできないのは「いま動いているセッション自身の記録」で、それが手動の存在理由になっている
D-11 | 自分の特定を fail-closed にする | 一致が 2 本のとき新しい方を採ると、別のウィンドウで進行中のセッションを途中で要約することになる。曖昧なら記録しない方が、打ち直し 1 回で済むぶん安い
D-12 | 空セッションにも印を残す | 未記録かどうかを日記だけで決めているので、「読んだが書くことが無かった」を残さないと、そのセッションが永久に候補へ挙がり続ける（実際に起きた）
D-13 | 記録係へのプロンプトを完成形で渡す | 「指示書を読んで従え」の形は auto mode の分類器に 17 回中 7 回拒否された。目的・番号付き手順・書き込む 1 ファイルを明示した形にすると通りやすい
D-14 | ナレッジの日付を Python が付ける | 日付を書かせると、モデルは transcript の中で見つけた日付を使い、3 行に 1 行は書き落とした。この日付は矛盾の解決に使われるので、誤りが表示上の問題では済まない

## 14 未確定事項
@rows
U-01 | macOS・Linux での動作 | コードに経路はあるが実行されたことがない | 実機で 1 セッション動かし、パスの区切りと時刻の扱いを確認する
U-02 | 分類器の通過率 | 呼び出し文を完成形にした後の通過率を測っていない。修正前は 17 回中 7 回が拒否 | 配信後の起動が積んだ試行を、権限ログと transcript の突き合わせで数える
U-03 | 検出が数える発言数 | フックからの通知やシステムメッセージも `type:"user"` として数えている。実測で、実発言 3 件のセッションが 5 件と数えられた | 空セッションの印を入れてもなお、記録に値しないセッションが候補に挙がるかを実測する
U-04 | `- [ ]` を使わないプロジェクトのタスク | 注入も消し込みも `- [ ]` の行だけを見るので、別の書式で書かれた項目は扱われない | 該当するプロジェクトで、書式を揃えるか読み方を広げるかを決める
U-05 | 記録係が `===TASKS_DONE===` に正しい行を逐語で書けるか | Python 側の照合は試験で固定したが、モデルがこの節を出す動作は一度も実運用で走っていない。逐語一致しなければ何も消えないので安全側には倒れる | 配信後の記録を数回集め、消し込みが実際に起きた件数と、一致しなかった件数を数える
U-06 | 記録係が指示から外れる頻度 | 日付の取り違えや既存項目との重複が観測されているが、頻度を測っていない | 配信後の記録を一定数集め、指示逸脱の割合を数える
