# ADR（設計判断の記録）

- 2026-07-22 セッション記録は「日記（diary/、時系列・引き継ぎ）」と「ナレッジ（knowledge/、永続知見）」の2本立て。理由: 引き継ぎ資料と蒸留知見は寿命が違う。捨てた案: 日記のみ（知見が日々に埋もれる）、ナレッジのみ（時系列の引き継ぎが失われる）、knowledge/handoff.md（日記のhandoff節と重複）。
- 2026-07-22 ファイル操作はすべてPythonラッパー（relay_recorder.py）が決定的に行い、モデルは区切り付きテキストを出力するだけ。理由: 安価モデルにファイル編集を任せたところ「追記」指示でも既存エントリを上書き消失させた（実バグ）。S番号・時刻・追記をコードに移せば喪失が構造的に起きない。捨てた案: モデルにEdit/Writeを許可する方式。
- 2026-07-22 記録はSessionEndフック→デタッチ起動のheadless Claude（既定 claude-haiku-4-5、read-only）。理由: セッション終了をブロックしない・利用枠消費を最小に。捨てた案: Stop hook方式（毎ターン発火のノイズ）。
- 2026-07-22 ナレッジの統合・矛盾解決はセッション開始時に本体Claudeが行う（終了時は単純追記のみ）。理由: 判断に迷う矛盾はユーザーがその場にいる開始時にしか確認できない。ホットループで統合しないのはメモリ設計の定石とも一致。
- 2026-07-22 注入はknowledge全文＋日記直近2ファイル全文（各knowledgeファイル約80行上限、超過は開始時に剪定）。理由: context rot対策は「小さく高シグナルに保って全文注入」が最も壊れにくい。捨てた案: handoff節のみ抽出（文脈が欠ける）、索引＋随時Read（安価モデルとの相性が悪い）。
- 2026-07-22 ナレッジ項目は「1項目1事実・命令形・[日付]付き・実際の失敗/訂正由来のみ・コード/CLAUDE.md/adrから導出可能なものは書かない」。理由: 指示が多いほど遵守率が下がるため高シグナル項目だけを蓄積する（CLAUDE.mdベストプラクティス知見）。
- 2026-07-22 時刻取得はWin32 GetLocalTime直叩き（relay_common.local_now）。理由: Git Bash由来の TZ=Asia/Tokyo をWindowsのCランタイムが解釈できずUTCに落ちる実バグを踏んだ。os.environからのTZ削除ではCRTに伝わらない。
- 2026-07-25 認証は既定でANTHROPIC_API_KEYを除去しサブスク認証で実行、RELAY_KEEP_API_KEY=1で保持に切替。理由: このキーは公式仕様上サブスクより優先され（headless -p では承認プロンプトも出ず必ず使われる）、無効キーが残った環境で記録係が401で沈黙死する事故が実際に起きた（log.txt: claude exited 129）。公式が案内する回避策もunsetのみで、優先順位を変える設定は存在しない。捨てた案: オプトイン方式RELAY_CLEAR_API_KEY=1（設定を書き忘れた環境が黙って壊れる）、除去手段なし（APIキー認証のみのユーザーを壊す）。
- 2026-07-22 対象範囲は既定全プロジェクト・RELAY_SCOPEで制限、プロジェクト無効化はRELAY_DISABLED=1。理由: 公開プラグインとして特定ディレクトリ構成を仮定しない。
- 2026-08-01 「現在の未完了」を表す第3層としてプロジェクト直下に status.md を追加（日記=時系列、knowledge=永続知見に次ぐ）。理由: 未完了事項が日記のhandoff節に散らばり、注入ウィンドウ（直近2日分）から外れると次セッションに届かなくなる。公式memory toolのmultisession patternもprogress_log（時系列）とfeature_checklist（現在の状態）の2本立てを推奨。捨てた案: knowledge/status.md（前置きの「重複・矛盾・80行超過を整理」「更新は上書き」指示がナレッジ全体に掛かり、本体Claudeがstatusを勝手に剪定する余地が残る）、diary/status.md（SessionStartがdiary/をYYYY-MM-DD.mdでマッチする実装に除外条件が要る）、日記のhandoff節をstatusに一本化（過去の日記から「その時点で何が残っていたか」が読めなくなる）。
- 2026-08-01 status.mdは追記ではなく毎回上書きで維持し、内容は未完了・保留・次アクションのみ（完了済みは書かない）。旧statusをプロンプトに渡して差分更新させ、今回のセッションで触れなかった項目は残す。理由: 完了済みを溜めると日記のdoneと重複して肥大する。「今回の内容だけで再生成」にすると、無関係な作業を1セッション挟んだだけで未完了事項が全滅し、この層が解決したい問題そのものが再発する。
- 2026-08-01 ===STATUS===セクションが空のときは既存status.mdを維持し上書きしない（「未完了なし」はモデルに明示の1行を書かせて表現）。理由: 上書き層では「モデルがセクションを出し忘れた」と「本当に全部終わった」が区別できず、前者を空扱いにすると未完了事項が黙って消える。relayは既に安価モデルの上書き消失で実バグを踏んでいる（2026-07-22の項）。捨てた案: 空を「全部完了」と解釈して素直に空にする。
- 2026-08-01 status.mdの上書き競合は台帳の.statusサイドカー（最後にstatusを書いたtranscriptのmtime）で順序保証し、より古いセッションのレコーダーはstatusを書かない（日記・ナレッジは従来どおり追記する）。理由: キャッチアップは最大3本のレコーダーを同時起動し、並行ウィンドウもあるため、古いセッションが後に完走して最新のstatusを潰す事故が上書き層固有に生じる。追記層では起きなかった問題。捨てた案: 単純な後勝ち、status.md内にHTMLコメントでメタを埋め込む（注入内容にノイズが乗りユーザーが手で消せる）、ファイルロック（同時実行は防げても「古い方が後に書く」順序問題を解けない）。サイドカーが壊れている・mtimeが取れない場合はフェイルオープン（statusを書く）とし、永久に更新されない状態を避ける。書き込みはtmp+os.replaceで原子的に行う。
- 2026-08-01 status.mdの書き手はレコーダーのみとし、セッション本体のClaudeは読むだけ（前置きに明記）。理由: 書き手が1つでないと順序保証が成立しない。「ファイル操作はすべてPythonラッパーが決定的に行う」（2026-07-22の項）の一貫。
- 2026-08-01 注入順はknowledge→日記→statusで固定（statusが末尾）。理由: statusは毎セッション全書き換えされ最も変わりやすいため、末尾に置くとセッション間のプレフィックスキャッシュ共有部分が最大になる。注入内容に現在時刻・セッションID等の毎ターン変わる要素は入れない方針も継続。
- 2026-08-01 showThinkingSummaries: true は対話モードでのみ有効、という検証結果にもとづき案3（thinkingをdecisions/mistakesの根拠に使う）を実装。理由: Claude Code本体（v2.1.220）のdisplay解決関数が `if(!isNonInteractive) return showThinkingSummaries() ? "summarized" : undefined; if(outputFormat==="text"||outputFormat==="json"&&!verbose) return "omitted"` であり、-p（headless・text出力）ではフラグが参照されず常にomittedに固定される。バンドル内に「set thinking.display to "summarized" … it is still block.thinking on a thinking block」の記述があり、レコーダーが読むフィールドに中身が入ることも確認。実地でも確認済み: relay-testプロジェクトの同一entrypoint(cli)・同一バージョン(2.1.220)・同一モデル(claude-opus-5)で、フラグ未設定のセッションが非空0/空14、フラグtrueのセッションが非空2/空0となり、jsonlのblock.thinkingに実際の中身（署名付き）が入ることを確認した。プロンプト側は「中身があれば使う・空なら何も変わらない」というデータ駆動記述にして、フラグ未設定のユーザーでも動作が変わらないようにした。捨てた案: transcript集計だけで結論を出す（entrypoint別にcli 0/2645・desktop 1021/88と割れたが、settings.jsonの過去版が残っておらず当時のフラグ値を復元できないため因果を決められなかった）、claude -p での A/B 検証（上記のとおりheadlessではフラグが参照されず、両者とも空になるだけで無効な試験だった）。
- 2026-07-27 記録の保証はSessionStart側のキャッチアップが担い、SessionEndフックは「即spawn」専用に縮小（thin判定・which判定はrecorder側へ移動）。理由: ウィンドウがclaude終了と同時に閉じる構成でSessionEndフックが完走前に殺され、記録対象セッションが無痕跡で失われる実バグ（Issue #1）。実測でフックの猶予は0.7〜1.5秒だがばらつきが大きく、ログ1行も残らないケースがあるため、フック内の並べ替えだけでは保証にならない。recorderは処理済みトランスクリプトのサイズを台帳（~/.claude/relay/ledger/<project>/<session_id>）に記録し、SessionStartで「30分以上更新なし・14日以内・台帳サイズと不一致」のものへrecorderを最大3本デタッチ起動する。導入時は既存トランスクリプトを記録済みとして台帳を初期化（過去セッションが今日の日記に化けるのを防ぐ）。resume後に成長したセッションはサイズ不一致で再記録される。捨てた案: フック内の処理順変更のみ（猶予700msに対し短縮できるのは10ms程度で今回のケースを救えない）、フック先頭での即spawn＋ガード省略のみ（フック不発・spawn前死亡を救えない）。
- 2026-08-30 日記を「1 transcript = 1 エントリ」にし、台帳（`~/.claude/relay/ledger/`）と初回採用を廃止。見出しは `## S1 19:11-22:30 (124c52dd)` でセッションIDと時間帯を持ち、エントリはそのセッション自身の日付のファイルに入る。transcript が伸びたらエントリを丸ごと差し替える。理由: 未記録かどうかが日記そのものから読めるため、二重記録を防ぐ外部状態が要らなくなる。resume で伸びた transcript が同じセッションの要約を2つ生む既存の欠陥も同時に消える。エントリが自分の日付のファイルに入るので「過去数週間分が当日の日記に化ける」が起きず、初回採用の仕掛けも不要になった。捨てた案: 台帳を残す（日記と二重管理）、見出しのIDをHTMLコメントに隠す（注入内容にノイズが乗り、ユーザーが手で消せる。2026-08-01 で同じ形を却下済み）。対立した要件: 追記のみの安全性 vs 1エントリの一意性——後者を採り、緩和は「transcript が伸びたときだけ」に限定した。
- 2026-08-30 relay 側に保持期間の窓を持たない（`CATCHUP_WINDOW_DAYS` を廃止）。存在する transcript はすべて記録対象で、1回の起動につき最大3本。理由: 窓があると「上限3本を新しい順に消化すると窓の外の古い分が永久に残る」「薄いセッションが最新のまま固定されると発掘が終わらない」という穴が生じる。境界を消せば境界の問題も消える。候補は記録するたび単調に減るので、順序の取り決めも要らない。transcript 自体は Claude Code が `cleanupPeriodDays`（既定30日）で消すため、実質そこが上限になる。対立した要件: 導入直後の利用枠 vs 記録の網羅性——後者を採った（発掘した日記は直近5件の注入窓に入らないので、価値は検索用途に限る）。
- 2026-08-30 記録手段をヘッドレス `claude -p` からセッション自身のサブエージェントへ全面置換し、`relay_recorder.py` を廃止。SessionStart が手順を注入し、本体が Task で実行する。理由: 環境制約でヘッドレスモードが使えない利用者が一切記録できなかった。タイミングの変更では解けず、手段を変える必要があった。サブエージェントへのプロンプトは「<ジョブのパス> を読み、指示に従え」の1行にし、長い指示本文はファイル側に置いて本体の文脈を汚さない。捨てた案: CLI があるときだけ従来経路（経路が2本になり試験も2本分）、環境変数で選択（既定値を選び損ねた環境が黙って壊れる。2026-07-25 で同型を却下済み）。対立した要件: 起動時の待ち時間 vs CLI 非依存——後者を採った。
- 2026-08-30 SessionEnd フックは残すが、記録はせず `~/.claude/relay/ended/<sid>` に空ファイルを1つ作るだけにした。判定は「印がある（かつ transcript が印の60秒後まで） または 30分以上更新なし」。理由: 記録を SessionStart に移すと、閉じてすぐ開き直したときに直前のセッションが30分ルールで拾えず、引き継ぎが落ちる。transcript から終了を読む手は無い（実測: 終了済み307本の最終行の型は `last-prompt` 264本ほか14種類にばらけ、しかもこの型は走行中でもターンの合間に最終行になる）。印はファイル作成1回で、書けなくても30分ルールに落ちるだけなので新しい失敗経路が増えない。捨てた案: しきい値を5分に下げる（数十秒の再起動は拾えず、並行ウィンドウを走行中に要約する危険が上がる）、最新1本だけ30分ルールを外す（並行ウィンドウと区別できない）。
- 2026-08-30 `status.md`（2026-08-01 に追加）を廃止し `knowledge/current.md` を採用、あわせて `knowledge/decided.md`（見送りと再開条件）を新設。`current.md` は現在地を書く1つの節で、上書き型・行数の上限なし。理由: 実測で `status.md` は17プロジェクトで0件、ログ1600行に記録0件だった。原因は「配信されていない」ことで、`plugin.json` / `marketplace.json` を 0.3.0 のままにしたため v0.4.0 が誰にも届いていなかった。層としては同一なので名前を1つにする。行数の上限を設けないのは、imagegen `tasks.md` の `## 現在地` が399行で腐っていない一方、同ファイルの未完了一覧45件は43件が死んでいたため——腐敗の分かれ目は上書きされるかどうかで、行数でも一覧かどうかでもない。捨てた案: `status.md` を正として `current.md` を作らない、両方持つ（4層目になり重複が増える）。
- 2026-08-30 記録のサブエージェントを2回に分ける（①transcript を読んで日記とナレッジ、②transcript を読まず現在地と見送り）。剪定は③として独立させ、`knowledge/` が80行を超えたときだけ起動する。理由: 出力契約を小さく保つほど節の欠落が起きにくい。②は①の書いた日記と既存ファイルだけを見るので transcript を読み直さず安い。剪定の発火条件を Python が計算することで、前置きの「気づいたら剪定する」（状態の記述であって手順ではない）が「条件を満たしたら起動する」に変わる。捨てた案: 1回の呼び出しで5節を出させる（`status.md` が節を落とされた疑いの形）、②③の起動判定を SessionStart 時点で行う（①が何も書かなければ②が空回りし、①の追記で80行を超えても③が発火しない）。
- 2026-08-30 日記の `handoff` 節は書き続けるが注入からは外し、注入する日記は日数ではなく件数（直近5エントリ）で切る。理由: `current.md` は handoff の蒸留なので、両方注入すると「どれが最新か」を探す作業がコンテキストの中で再現する。日数で切ると、その期間に何セッションあったかで注入量が3倍変わる（実測: 直近2日分がプロジェクト間で610〜10,518字）。handoff をファイルに残すのは、過去の日記から「その時点で何が残っていたか」が読めなくなるのを避けるため（2026-08-01 で同じ理由から一本化を却下済み）。対立した要件: 重複の除去 vs 点での記録の保存——書くのは残し、注入だけ外すことで両立させた。
- 2026-08-30 Stop フックを新設し、未記録の transcript が残っているときだけ記録を促す（プロジェクト単位で30分クールダウン、公式の `stop_hook_active` ガード）。判定は SessionStart と同じ `pending_sessions()` を呼ぶ。理由: 記録がセッション自身の実行に依存するようになったため、本体が注入手順を無視すると記録が黙って落ちる。デタッチ済みプロセスが持っていた保証を埋め戻す唯一の口になる。走行中の自セッションは終了判定で自動的に外れる。
- 2026-08-30 ANTHROPIC_API_KEY の除去（2026-07-25 の項）と `RELAY_KEEP_API_KEY` を廃止。理由: relay が子プロセスの `claude` を起動しなくなり、キーを外す責任が relay の範囲から出た。**あわせて 2026-07-25 の記述自体を訂正する**——「無効キーが残ると記録係が401で沈黙死する」は誤りで、2026-08-12 に「キーは有効で実際に課金される（kuchu-saiban で約$14 の実害）」と訂正済み。危険が世界から消えたのではなく、relay が引き起こす経路が無くなっただけである。
- 2026-08-30 リリース時は `plugin.json` と `marketplace.json` の両方でバージョンを上げ、push 後に `~/.claude/plugins/installed_plugins.json` の version が実際に変わることまで確認する。理由: v0.4.0（`f0e5043`）はこの両ファイルを 0.3.0 のままにしたため `claude plugin update` が新版と認識せず、書いたコードが1か月間まったく配信されなかった。配信の成否は、配信した側ではなく受け取った側でしか確かめられない。
- 2026-08-30 v0.5.0 導入時、既存の日記に id 付き見出しが1件も無くても既存 transcript を全数「未記録」として遡る（実測: `RELAY_SCOPE` 配下の日記216ファイル・見出し770件のうち id 付きは0件で、transcript 232本すべてが `mode="new"` になる）。同じ日に旧 `## S1 10:24` と新 `## S2 09:12-11:40 (id)` が並ぶことは受け入れる（`relay_apply.py` の `num = len(entries) + 1` が id 無しも数えるため S1 が2つにはならない）。理由: 発掘は一度きりで、1起動3本なら約77起動で解消し以後発生しない。掘るのは「掘った日記が読まれるから」ではなく、掘らない限り毎起動で232本が未記録と判定され続けるからで、発掘分の価値は検索用途に限る（セッション自身の日付のファイルに入るため直近5件の注入窓には乗らない）。捨てた案: id 無しのエントリを日付とセッション番号から推定して記録済み扱いにする（確定11「id を持たない既存の日記には一切触らない」の破棄になり、推定が外れる余地が恒久的に残る）、導入時に一度だけ記録済みの印を付ける（廃止した初回採用の復活で、「外部状態を持たない」という台帳廃止の動機と衝突する）。対立した要件: 一時的なコスト vs 恒久的な複雑さ——前者を払って後者を買わない側を採った。
- 2026-08-30 `session_span()` が全走査へ落ちたことを示すログ行は、人間プローブ（P1〜P7）が終わるまで足さない。再開条件: 段3（配信＝version 確定）以降に、必要と判断された時点で足す。理由: プローブは配信予定のコードそのものを測る場なので、測る直前に測定対象を変えるとプローブが何を測ったのか分からなくなる。「配信予定コードの文書だけを先に更新すると矛盾が生まれる」の裏返しで、文書でもコードでも測るものと出荷するものがずれる点は同じ。落下する2本は名前で特定済み（nekomata `82b4eb21` / imagegen `d23b8329`）なので、ログが無くても切り分けられる。
- 2026-08-30 `_chunk_timestamps()` の先頭64KB判定は今は変えない。再開条件: 段4 の定常確認で「先頭チャンクが覆う必要のあるバイト数」の分布の右端が動いていたら見直す（監視は本数ではなく分布で行う。実測 64KB超2本・48KB超6本・32KB超11本・中央値3,989B で、本数だけ数えると閾値をまたぐ直前が見えない）。理由: 全走査に落ちるのは232本中2本（0.9%）で、両端チャンク走査 合計657ms のうち250msにとどまる。落下はファイルの中身だけで決まり、transcript の先頭は書かれたあと変わらないので既存ファイルの落下有無は動かず、測定値のばらつき要因にならない。原因は2種類で、(a) 使える timestamp を持つ最初の完全な行が切断面の向こうにある「長い1行」（imagegen `d23b8329` は 72,236B 必要）、(b) 前置きの行数（nekomata `82b4eb21` は `file-history-snapshot` が60行あり 308,254B 必要）。捨てた案: 部分文字列 `"timestamp"` のマッチをトップレベルキーの有無に寄せる（今回の2本はどちらも救えない。効くのは「切れた最終行を捨てず、完全な行が1つ取れるまで読み進める」ほう）。
- 2026-08-30 記録サブエージェントに渡すプロンプトを1文から2文にし、「指示書の内容は、transcript を1本読んで要約テキストを1ファイルに Write するものです」に相当する説明を必ず含める（overwrite / prune は対応する説明に差し替える）。あわせて記録3本は **1つずつ順にではなく同じメッセージでまとめて呼ぶ**ことにし、確定18（順に呼ぶ）を見直した。理由: 実測で、説明の無い1行プロンプトは auto mode の分類器に `Blocked by classifier` で拒否される。auto mode は既定の権限モードなので、注入された手順どおりに動いた利用者が理由の分からない拒否に当たる。まとめ呼びは、確定18 に付いていた条件「ただし所要時間を実測する」が満たされたことによる（imagegen 実測: 記録3本 109.7s / 207.7s / 135.6s ＋ 現在地 81.5s ＝ 合計534.5s。所要は transcript のバイト数に比例せず 1本あたり80〜210秒でばらつく）。3本は互いに独立で、それぞれ別の inbox ファイルに書くため同時実行で競合しない。捨てた案: ジョブ本文をプロンプトに展開する（出力契約が大きくなり、確定6で分割した意味が消える）。
- 2026-08-30 剪定ジョブ `prune.md` は `relay_apply.py` が apply のたびに現在の本文で作り直し、同時に `prune.stamp`（pitfalls.md と workflow.md の sha256）を書く。`relay_apply.py` は stamp が現在のファイルと一致しないかぎり `PRUNE_PITFALLS` / `PRUNE_WORKFLOW` を適用せず、要約行に `stale=<件数>` を出す。理由: 実データ損失のバグ。`prune.md` は SessionStart が生成して以後再生成されないのに、手順は「記録→apply（＝pitfalls/workflow へ追記）→その後で剪定」の順である。剪定は全文置換なので、ジョブ生成後に追記された項目は剪定係の入力に無く、出力にも現れず、痕跡なく消える（imagegen 実測: 埋め込み156行 vs 実ファイル166行で、記録3本が足した10項が対象。inbox の txt は適用後に必ず unlink されるため復元元が実ファイルしかない）。再生成が原因を消し、stamp は順序が崩れた場合の最後の砦。捨てた案: 剪定係に差分だけ返させる（出力契約が複雑になる）、`write_jobs()` の中でジョブ生成を後ろに動かす（hook 時点で全ジョブを書き終える設計と衝突する）。対立した要件: 剪定の実行しやすさ vs 追記の保全——後者を採り、一致しないときは剪定を飛ばす側に倒した。
- 2026-08-30 日記の見出しは日付をまたいだ日数を持つ（`## S2 08:14-08:13+3d (00cb0081)`）。`ENTRY_RE` に `(?:\+(\d+)d)?` を足し、日数があればそれを加算、無ければ従来どおり「終了が開始より前なら+1日」で読む。理由: 表示だけの問題ではない。日数が無いと `parse_diary_file` が3日の span を+1日と読み、エントリの終了時刻が2日ぶん過去になる。すると `pending_sessions()` が transcript を「エントリより新しい」と判定し続け、**そのセッションが毎起動 replace で再記録される**（15日の resume 窓にも掛からないので永久に収束しない）。imagegen で `00cb0081`（08-26 08:14→08-29 08:13）と `6531a727` が該当。捨てた案: 見出しに開始日と終了日を両方書く（既存エントリと書式が変わり、日記ファイル名と日付が重複する）。
- 2026-08-30 剪定ジョブを「統合ジョブ」に変え、**行数のための削除を禁じる**。`PRUNE` から「各ファイルを80行以内に収める」を外し、「項目を削除しないこと」を明記。80行は上限ではなく「ここが育った」という合図として残し、超えたときは `===MOVE===` 節に移設候補を理由付きで挙げさせ、`relay_apply.py` が日記の `## relay: knowledge/ の移設候補` に追記する（移設先も可否も人が決める。この見出しは注入の直近5件に数えない）。発火条件は「行数超過 かつ 前回の統合から中身が変化」に変え、要約行の `merge_due=yes|no` を唯一の判定にした。理由: imagegen 実測で pitfalls 130項目→43項目、workflow 80→35項目まで削られ、落ちた92件は重複でも古い項目でもなく一次情報だった（pHash のしきい値6、CLIP の near ペア AUC 0.479、Danbooru 10req/秒 など）。統合能力の問題ではない——用意された重複1組は正しく1行になっていた。入力は166行＋104行で重複が実質1組しかなく、80行に統合では届かない以上、従えば削除するしかない構造だった。「80行以内」は読み込みコストを抑える手段だったが、その手段が剪定の目的（一次情報を残す）を殺していた。発火条件に変化を混ぜたのは、削除しない以上ファイルが上限を下回らず、行数だけを条件にすると大きいプロジェクトで毎起動発火し続けるため（実測: 剪定込み1起動 12分10秒）。捨てた案: 上限を200に上げる（実測でどのプロジェクトも発火しなくなり「上限を上げる」と「剪定を止める」が同じ結果になる。imagegen 166行/104行、次点 freetalk 54行/73行）、剪定の発火自体を取り下げる（0.3.0 と同じく一度も統合されない状態に戻る）。対立した要件: 注入量の抑制 vs 一次情報の保全——後者を採った（注入量は時間にほぼ影響しないと実測済み。80,117字と2,257字で同じ約350ms）。
- 2026-08-30 記録ジョブを呼ぶプロンプトは SessionStart が**完成形で書き出し**、本体はそれを丸ごと貼る。形は「目的の1文 → 番号付きの手順（各手順に使う道具名）→ 書き先1本だけという制約」。2文形式（確定27）はこれに置き換える。理由: 実測で足りなかった。`~/.claude/permlog.jsonl` の `PermissionDenied` 25件と transcript の `tool_use` を `tool_use_id` で突き合わせると、relay のサブエージェント呼び出しは17回中7回が `Blocked by classifier`。2文形式は3回中2回拒否され、**同じプロンプトの再送で結果が変わる**（10:33:14 拒否 → 10:34:58 通過）。段落で説明した形も2回拒否され、通ったのは10:44:19 の1回——目的から始め、手順に番号を振り、各手順の道具（Read / Write）と書き先1本を名指しした形だった。分類器は確率的でこちらから制御できないので、**通過率を上げる形に寄せるのが唯一できることで、100%は保証しない**。捨てた案: 拒否されたら同じ呼び出しを再送する（実測では通ることがあるが、拒否メッセージ自身が「他の道具で目的を達成してよい／拒否の回避を試みるな」と言っており、同一呼び出しの連打はその線を踏む。拒否されたセッションは未記録のまま残り、次の起動でまた候補に上がるので記録は失われない）、ジョブ本文をプロンプトに展開する（確定6・確定27 と同じ理由で却下）。
- 2026-08-30 `relay_apply.py` を呼ぶ手順に「**この1行をそのまま実行し、`cd` を前置しない・`&&` や `;` で連結しない・`2>&1` を足さない**」を明記する。理由: 実測で `cd "C:/claude-projects/kuchu-saiban" && python "...relay_apply.py" --cwd "..." 2>&1` が拒否された（permlog 2026-08-30T19:40:58）。`cd` を外した形は通っている。DENY 25件のうち Bash は18件で、その多くが `cd <dir> && <変更を伴うコマンド>` の連結形。`apply_cmd` 自体は `cd` を含まないが、モデルが自分で足す余地があり、手順で塞ぐ以外に止める場所がない。作業ディレクトリは `--cwd` で渡してあるので `cd` は要らない。
- 2026-08-30 knowledge 項目の `[日付]` は **Python が刻み、モデルには書かせない**（`- [YYYY-MM-DD]` の前置は `relay_apply.knowledge_bullets` が付ける）。値は**そのセッション自身の日付**。理由: 実測でモデルは渡された `{date}`（＝今日）ではなく transcript 内の日付を使い、`workflow.md` に足した3行のうち1行は日付を丸ごと落とした。`[日付]` は統合ジョブが「矛盾する項目は新しい方を正とする」判定に使い、前置きも「[日付] が古い項目に依拠する前に実態を確認する」と言うので、誤りや欠落は表示の問題ではない。値をセッション自身の日付にするのは、遡り記録（transcript 232本）が全部「掘った日」で刻まれると古い知見が一律で最新になるため——日記のエントリが自分の日付のファイルに入るのと同じ基準に揃える。捨てた案: プロンプトで日付を強く指示する（`## S1 ...` の見出しを Python が付けるのと同じ理由で、守らせるより奪うほうが確実）。記録した日で刻む案は捨てた（遡り記録232本が全部同じ日付になり、「矛盾は新しい方を正とする」も「古い項目は実態を確認する」も比較の軸を失う）。**あわせて「モデルが transcript 内の日付を使ったのは逸脱」という段2c の報告を訂正する**——正しかったのはモデルのほうで、直すべきは `{date}` に `local_now()` を渡していた指示のほうだった（freetalk と合意）。
- 2026-08-30 ナレッジ項目の重複の抑制は**入口のプロンプトだけで行い、文字列類似度による自動判定は入れない**。`RECORD` の規則を「この2節は空のまま終わるのが普通」「既存項目と**同じ主題**のものは言い回しを変えても書かない」に変え、実例を1つ添えた。理由: 閾値が存在しない。kuchu-saiban の実データで既知の重複4組の difflib 比は 0.370〜0.479、無関係な最上位ペアは 0.353 で範囲が重なる（言い換えによる重複なので当然）。誤検知の代償は一次情報の無音の消失で、80行問題（確定29）と同じ失敗の再来になる。統合ジョブは重複を正しく1行にできると実測済みだが、発火条件が80行超なので小さいプロジェクト（kuchu-saiban は12行）では一度も走らない。捨てた案: 統合ジョブを毎回走らせる（1起動あたりのサブエージェントが1本増え、そのぶん分類器に拒否される機会も増える）。
- 2026-08-30 分類器の通過率は**実ログから数える**（1回通っただけでは確認にならないため）。分母＝`~/.claude/projects/**/*.jsonl` の `tool_use` のうち `name` が `Task`/`Agent` で `input.prompt` に `/relay/jobs/` を含むもの、分子＝`~/.claude/permlog.jsonl` の `DENY` 行を `tool_use_id` で突き合わせたもの。修正前の実測値は 17回中7回拒否（拒否率41%）。`permlog.jsonl` はユーザー環境の `PermissionRequest` / `PermissionDenied` フックが書いており、relay の一部ではない——**この測り方は開発環境でのみ成立する**。配信後は 77プロジェクトの起動が自然に試行を積むので、同じ突き合わせを後から回せば母数の大きい通過率が出る。
- 2026-08-30 配信の経路を3点で確定し、2026-08-30 の「両ファイルの version を上げ、`installed_plugins.json` の version を確認する」を置き換える。(1) marketplace は GitHub リポジトリの **main しか読まない**ので、ブランチのままの push では届かない。(2) `claude plugin update <plugin>` の前に `claude plugin marketplace update <name>` でマーケットプレイスのクローンを更新する。(3) 届いたかの確認は version 文字列ではなく `installed_plugins.json` の `gitCommitSha` が動いたかで見る。理由: 現物確認——`~/.claude/plugins/marketplaces/relay` の fetch refspec は `+refs/heads/main:refs/remotes/origin/main` で main 以外を fetch すらしない。`known_marketplaces.json` の relay エントリは `{"source":"github","repo":"shiyu9/relay"}` だけで ref を指定する場所が無い。そのクローンは `ce80cae`（v0.3.0）・`lastUpdated` 2026-07-27 で止まっており、main にある v0.4.0 すら取得していない——**「届かない」は version 据え置き以外の経路でも起こる**。`gitCommitSha` は取得した中身から決まるので、「version を上げ忘れた」と「クローンが古い」を1つの確認でまとめて検出できる。捨てた案: `claude plugin tag` で事前検査する（`plugin.json` と marketplace エントリの version 不一致を検証するコマンドで、v0.4.0 の死因はこれで防げた。ただし今回は両方 0.5.0 で一致しており検出対象が無く、タグを作る副作用だけが残る。version が食い違っているときの手段として記録に留める）。
- 2026-08-30 導入時点より古い transcript は「記録済み」として記録対象から外す（epoch）。判定は `relay_detect.pending_sessions()` が Python で行い、`transcript の末尾 timestamp <= epoch` を候補から落とす。理由: relay は既に数週間分の transcript を抱えたプロジェクトへ導入される。v0.5.0 が見出し書式を変えて v0.3.0 の記録を「未記録」と見なした結果、12プロジェクトで計86件・約394MB が pending となり、起動ごとに3件ずつ重複記録・quota 消費・current.md の巻き戻りが続いた（実測）。プロンプトではなく Python 側の判定にしたのは、記録するかどうかがモデルの善意に依存してはならないため。捨てた案: このまま消化を許す（コストと重複を無期限に払い続ける）、旧形式の見出しに sid を後付けする移行処置（対応付けを1件でも誤ると黙って永久スキップになる／書式の意味論を外すと永久再記録を自作する／日記が唯一の状態であるところへ一括手術を掛ける）。
- 2026-08-30 epoch はプロジェクトごとに1回だけ書き、以後けっして動かさない（`~/.claude/relay/epoch/<sanitized-cwd>`・ISO 日時のテキスト1行）。理由: 更新のたびに引き直す線は「保持の窓」であり、v0.5.0 の設計が明示的に捨てた形そのもの（窓の外へ後から落ちる経路が生まれ、抜け漏れ A・B・C・E は全部これが原因だった）。一度きりの線には落ちる先の外側が無い——epoch より新しくなったセッションは永久に新しいままになる。したがって依頼文の「導入・更新時点」は「epoch 機構がそのプロジェクトで初めて走った時点」と解釈する。捨てた案: プラグイン更新のたびに epoch を現在時刻へ進める（動く窓に戻る）。
- 2026-08-30 epoch は transcript の**末尾**の timestamp と比べる（開始時刻ではない）。理由: 導入時にまだ走っていたセッションは末尾が線を越えて伸びるので記録される。古い transcript が resume で伸びた場合も同様に候補へ戻る。開始時刻で比べると、線を跨いで走っていたセッションが丸ごと落ちる。
- 2026-08-30 epoch は「backlog」と「今日閉じたばかりの未記録セッション」を区別しない。線より前に終わっていた transcript は、それが何であれ記録済みとして扱われる。理由: 区別できる材料が無く、閾値を足せばそれは動く窓に戻る。ユーザーはこの帰結を理解した上で②を選んでいる（2026-08-30 夜の直接決定）。開いているセッションは末尾が動くので安全側に落ちる。
- 2026-08-30 epoch は `pending_sessions()` の**最初**に引く（transcripts ディレクトリの存在確認より前）。理由: transcripts ディレクトリがまだ無いプロジェクトは、relay が最初のセッションから居合わせているプロジェクトである。そこで線を引き損ねると、次の起動で引かれた線がその第1セッションを backlog として切り捨てる。
- 2026-08-30 epoch ファイルが読めない・日付として解釈できないときは「線なし」として全件を候補に戻す（フェイルオープン）。理由: ファイルは人が編集して線を動かすための平文で、打ち間違いが86件分の記録を黙って消してはならない。洪水は見えて回復できるが、黙って飛ばされた記録は痕跡が残らない。`RELAY_EPOCH` 環境変数で明示指定（ISO 日付/日時）と無効化（`none`/`off`/`0`）ができ、この場合ファイルには触れない。
- 2026-08-30 `plan_entry()` には epoch を効かせない。理由: `pending_sessions()` は「記録するかどうか」、`plan_entry()` は「既に書き出された成果をどこへ置くか」を答える。サブエージェントが既に要約を作り終えているものを、線を理由に捨てるのは無駄でしかない。
- 2026-08-30 Stop hook は促すたびにジョブを組み直し、ジョブ置き場を指すのではなく手順そのものを reason に載せる（`relay_jobs.write_jobs()` を SessionStart と共用）。理由: 起動時に書かれたジョブは「起動時点で pending だった transcript」を名指しする。それを消化した1時間後のセッションでは、ジョブ置き場には記録済みセッションの指示書しか無く、促しどおりに動くと古いジョブの再実行になって実際の未記録分は記録されない（2026-08-30 実地で発見）。起動時の手順がコンテキストから落ちている可能性もある。
- 2026-08-30 Stop hook は `jobs/<sid>.md` の mtime が15分以内の sid については促さない（`relay_common.jobs_in_flight()`）。理由: 記録サブエージェントは Python から見えず、終了も通知しない。受け箱にファイルが無い状態は「未着手」「実行中」「適用済み」の全部を含むので、ジョブ指示書の更新時刻だけが唯一の手掛かりになる。並行記録ジョブの実行中に「未記録3件」と促す事故が実測されている。判別は完全ではないが、誤って促した場合の害は quota の無駄だけで、`upsert_entry` は sid で置換するため日記は壊れない。捨てた案: 受け箱のファイル有無で判定する（3状態を区別できない）。
- 2026-08-30 Stop hook の in-flight 判定は、ジョブを組み直す**前**の mtime で行う。理由: 組み直しは mtime を更新するので、順序を逆にすると促すたびに自分で猶予をリセットし、永久に黙るか逆に黙るべきときに促すかのどちらかに倒れる。`test_the_in_flight_question_is_asked_before_the_jobs_are_rebuilt` がこの順序を固定している。促す対象も outstanding だけに絞る（実行中のジョブを書き直すと、その猶予を理由なく延長してしまう）。
- 2026-08-30 現在地ジョブに渡す日記は「今回記録した分 ∪ 直近5エントリの日付」にする。理由: 材料が今回記録した分だけだと、古いセッションを遡って記録した回は何週間も前の handoff から現在地が書き直される（2サイクル連続で再現。2回目は「配信はユーザー判定待ち」——実際は配信済み——と書いた）。epoch でバックログ自体は消えるが、resume 判定で古いセッションを記録するたびに同じことが起きるので、材料の側を直す。
- 2026-09-01 タスク管理（資料整理プラン第4段 4-2）で relay が担うのは `tasks.md` の**消し込みだけ**とし、項目の追加は担わない。消し込みは既存の現在地ジョブに `===TASKS_DONE===` 節を足して行い、新しい発火点を作らない。理由: `tasks.md` は既に各プロジェクトのセッション（Claude）が書いており、ユーザーはこのファイルを読んでいない——壊れているのは消し込み側だけで、実物でもそこだけが腐っている（imagegen に `- [x]` が9件・kuchu-saiban に1件残存。2026-08-30 の項の「未完了45件中43件が死んでいた」も同型）。現在地ジョブは transcript を読まず日記と `knowledge/` だけを見るので、材料に `tasks.md` を足す増分が小さい。捨てた案: Stop hook + サブエージェントで書かせる（2026-08-28 のユーザー決定。Stop はターン完了を同期的にブロックし、判定コストはウォームで 68〜252ms、コールドで freetalk 9838ms を1回観測（n=1・再現未確認）。拾えるのは1セッションに0〜数回で、しかもタスクの追加は Stop hook 無しで既に回っていた）、独立した4つ目のジョブ（読む材料が現在地ジョブとほぼ重複し、auto mode の分類器を通す回数だけ増える）。対立した要件: 消し込みの即時性 vs 発火点を増やさないこと——後者を採り、消し込みは1ターン遅れる（SessionStart の注入には消し込み前の状態が入り、直後に走る記録手順で消える。記録は「ユーザーへの最初の応答より前」に走る決まりなので実作業に入る時点では消えている）。
- 2026-09-01 消し込みの根拠は「同便で確定した日記の `done`」に限り、`===TASKS_DONE===` 節の欠落は `bad` ではなく no-op として扱う。消す行は Python が `tasks.md` 上での実在と形式を検証してから消し、消した項目は継続行ごと日記の `## relay: 完了として tasks.md から消した項目` に退避する。理由: 現在地ジョブは transcript を読まないので、根拠を日記の `done` に限れば削除の全件が日記から追跡できる。節の欠落を no-op にするのは、欠落しても「消し込みが1回飛ぶ」だけで次のセッションが再び候補にするため——`current.md` の空節（2026-08-01 の項）と違って情報が消えない。退避があっても検証は緩めない（復元できることは誤削除の許可ではない）。捨てた案: `- [x]` に書き換えて残す（imagegen に9件溜まっているのがその形の末路）。
- 2026-09-01 案A（日記の `handoff` は状態のみにし、着手中はタスク側に `[進行中]` として残す）は 4-2 の時点でも採らない。理由: v0.5.0 の `session_start_hook.py:strip_handoff()` が `handoff` を**注入時にだけ**落としており（ファイルには残る）、案Aが解こうとした「何が残っているかの一覧が複数注入される」重複は実装で既に解決している。2026-08-01 の却下理由（日記の `handoff` を一本化すると過去の日記からその時点の未完が読めなくなる。しかも不可逆）は今も生きている。2026-08-29 に「タスク側が実在するようになったら再検討する」と保留したが、再検討の結論は現状維持で変わらない——ただし理由は「利益が出ない」ではなく「利益は実装済みで、損失だけが残る」に変わる。
- 2026-09-01 `tasks.md` の `- [ ]` 行（継続行と直近の見出しを含む）を SessionStart の注入に加え、位置は日記の後・`current.md` の前とする。理由: 注入順は「変わりにくいものから変わりやすいものへ」（2026-08-01 の項）で、`tasks.md` は消し込みが起きた回だけ変わるので、毎回全書き換えされる `current.md` より変わりにくい。抽出するのは `- [ ]` の行だけで書式は強制しない——実測で8プロジェクト全部が `- [ ]` を使っており、抽出後は 4〜86行（imagegen が最大で 86行・4512字）。捨てた案: `tasks.md` の全文を注入（imagegen 159行・claude-memory 154行が丸ごと乗る）。`- [ ]` を使っていない項目が多いプロジェクト（cashlan は111行中4行しか拾えない）では注入が薄くなるが、書式を強制しない以上そこは拾わない。
- 2026-09-01 `tasks.md` と `knowledge/decided.md` の分別を「やると決まっているが着手していない → `tasks.md`」「再開条件はあるが、やると決まっていない → `decided.md`」と定義する。理由: 両者とも「再開条件つきで書かれた未完項目」の形を取りうる（rally-dev の README 申し送りにある「未了（2026-09-01 起票）」2件が実例で、`decided.md` の書式と見分けがつかない）。この定義なら `decided.md` の既存規則（再開条件のある見送りだけを書く）ともプランの4分類とも衝突しない。
- 2026-09-01 Stop hook の未記録促しに3つの問題を確認した（4-2 とは独立の改修として扱う）。(1) `pending_sessions()` を毎ターン全走査しており、ウォームで 68〜252ms、コールドで freetalk 9838ms を1回観測（n=1・再現未確認）。Stop はターン完了を同期的にブロックするので、そのまま待ち時間になる。(2) auto mode で分類器に拒否された場合、手順自身が「再送せず次の起動へ回す」と指示しているため、そのセッション中は原理的に解決しないのに30分おきに促しが出続ける。(3) `JOB_GRACE_SECS`(15分) と `COOLDOWN_SECONDS`(30分) が暗黙に結合している——Stop hook は促すたびに `write_jobs()` で job の mtime を更新するので、猶予を30分超に変えると促しが永久に止まる。
- 2026-09-02 Stop hook を完全撤去し（`hooks.json` の登録と `relay_stop_hook.py` の両方）、記録は「セッション終了時に人が手動スキルで実行する」を基本、「忘れたら次の SessionStart が拾う」を保険とする2段構えにする（ユーザー決定）。理由: 2026-09-01 の項に挙げた3問題（毎ターンの全走査・分類器に拒否され続けると解決不能なまま30分おきに促しが出続ける・`JOB_GRACE_SECS` と `COOLDOWN_SECONDS` の暗黙の結合）が、hook ごと消える。2026-08-28 の「Stop hook を足す」決定は完全撤回になるが、その目的だったタスク管理は既に記録経路へ移っており（2026-09-01 の項）、目的ごと消えている。撤去にともない `STOP_NUDGE` / `hook_state_dir()` は参照元が無くなる。`jobs_in_flight()` と `JOB_GRACE_SECS` は本番コードの参照が Stop だけなので去就は未確定（手動実行の多重起動を防ぐ用途が残るかを検討中）。
- 2026-09-02 手動スキルの実行時は、自分自身のセッションも記録対象にする（`_ended()` の「印がある / 30分以上更新なし」を迂回する）。迂回するのは特定できた自分1本だけで、他のセッションは現行判定のまま——全部を迂回すると別ウィンドウで走行中のセッションを途中で要約することになり、2026-08-30 の項が「30分」を選んだ理由をそのまま壊す。自分の transcript は**実行中のコマンド文字列で特定する**。実測: 一意なトークンをリテラルで含む `python -c` を実行し、そのスクリプト自身に `~/.claude/projects/<project>/*.jsonl` を走査させたところ、自分の transcript だけが1件ヒットした——**ツール呼び出しは実行時点で既に transcript に書き込まれている**。捨てた案: スクラッチパッドのディレクトリ名から session_id を得る（この環境では一致を確認したが、環境依存の可能性があり当てにしない）。トークンを使い回すと過去の transcript にも残って複数ヒットするため、時間窓と最新1本で絞る方向で検討中（未確定）。
- 2026-09-02 レコーダーが `NOTHING_TO_RECORD` を返したセッションにも、日記に見出しと1行の印（`（記録するものがなかったセッション）`）を書く。この印を持つエントリは注入と現在地ジョブの材料から外す。理由: 未記録かどうかは日記だけを見て決めているので（2026-08-30 の項で台帳を廃止した）、「読んだが書くことが無かった」を日記に残さないと、そのセッションは永久に候補へ挙がり続ける。freetalk で実際に起きた——4分・実発言3件のセッションが記録され、`NOTHING_TO_RECORD` が返り、`relay_apply` が log 1行だけ残して捨て、30分後にまた促された。分類器拒否と同型の「セッション内で解決不能なまま繰り返す」がもう1系統あった。注入から外すのは、空のセッションが直近5件の枠を食わないため。捨てた案: 外部に「記録済み」の台帳を持つ（2026-08-30 が廃止した形に戻る）、`log` だけで済ませる（現状であり、これがループの原因）。
- 2026-09-02 検出側の `count_user_messages` は合成メッセージ（hook feedback・システム通知）も user 行として数えており、実発言の数と一致しない。実測: freetalk の `0846e4a6` は user 行5件のうち実発言3件で、1件は**relay 自身の Stop hook 促し**だった——促すほど記録対象らしくなる自己参照になっていた。Stop hook の撤去でこの経路は消え、`NOTHING_TO_RECORD` の永続化でループも止まるため、合成メッセージの除外は入れない（入れると「記録に値するか」の判断が2箇所に分かれ、検出とレコーダーがまた食い違う）。再開条件: 永続化を入れてもなお、記録に値しないセッションが候補に挙がることが実測で分かったとき。
- 2026-09-02 配信物のバイト照合（2026-08-30 の項）は、改行コードを正規化してから比べる。新規ファイルを LF で作ってコミットすると配信先（git clone）では CRLF になり、生バイトの比較が「不一致」と出るため。実測: v0.7.0 で `relay_record.py`（2667→2745 バイト・CRLF 78 箇所）と `skills/record/SKILL.md`（2949→3003・54 箇所）が生バイトで不一致、正規化すると一致。既存ファイルは CRLF でコミット済みなのでそのまま一致する。中身の差ではないので実害はないが、照合手順をそのまま当てると誤検知になる。
- 2026-09-02 記録係が長い transcript の前半を落とすことを実測した。指示書の「JSONL です。大きい場合は末尾を優先して読んでください」が原因で、7 時間・1.5MB 超のセッションを記録させたところ、要約が捉えたのは終盤の 1 項目（仕様書の artifact 公開）だけで、同じセッションで行った 4-2 の設計・タスク消し込みの実装・Stop hook の撤去・v0.7.0 の配信がすべて落ちた。`handoff` も既に解決済みの状態を「未解決」と書いた。**対処は未決。**候補は「末尾優先の指示を外す（長い transcript で読み切れずに時間と枠を使う）」「セッションを分割して読ませる（出力契約が複雑になり、2026-08-30 の項が避けた失敗に戻る）」「要約の網羅性を検査する手段を持つ（何をもって網羅とするかが決まらない）」で、いずれも別の代償があるため、まず頻度を測る。長いセッションほど落ちるので、relay 自身の開発のような長丁場で最も効かなくなるという逆進性がある。
- 2026-09-02 **訂正: 2026-07-27 の「フックの猶予は 0.7〜1.5 秒」はコンソール破棄でもばらつきでもなく、`timeout` 未宣言のときの既定値 1500ms だった。**実測（Windows・CLI 2.1.252・全 42 セッション）: settings.json に `"timeout": 30` を宣言した SessionEnd hook は 10 秒走り切り、同じ終了で隣の未宣言 hook は 1.0〜1.5 秒で切られた。終了経路（`/exit`・Ctrl+C・窓閉じ）による差はゼロ。当時この観測を根拠に「フック内の並べ替えでは救えない」と判断して SessionEnd での記録を諦めたが、原因の取り違えだった。現象は正しく観測していたので、次に同じ形の誤りを避けるために訂正として残す。
- 2026-09-02 **hook が時間切れになると、その hook のプロセス木が辿られて子孫ごと殺される。時間内に終われば何も殺されない。**実測: 親が 10 秒走ろうとして 1500ms で切られたとき、`DETACHED_PROCESS` で産んだ子も `CREATE_BREAKAWAY_FROM_JOB` を足した子も 1〜2 秒で死んだ（15/15）。親が即 exit する版では同じ子が 30 秒生き残った（3/3）。BREAKAWAY が効かないので犯人は Job Object ではなく、木を辿る側。唯一生き残ったのは `CreateProcessW` を `PROC_THREAD_ATTRIBUTE_PARENT_PROCESS` つきで呼び、生まれた時点で explorer.exe の子にした場合（30 秒・全セル 5/5）。
- 2026-09-02 **plugin の hooks.json に書いた SessionEnd の `timeout` は、値によらず無視される。**実測: 5 でも 30 でも既定 1500ms のまま（6/6）。同じ書き方を settings.json に置くと効く。SessionEnd 自体は plugin 経由でも発火するので #33458 には該当しない。**relay は plugin として配信されるので、猶予を自分で買えない。**1.5 秒は relay が自分で守る上限として設計に書く。宣言は hooks.json に残す（settings で上書きする利用者に効き、害は無い）。
- 2026-09-02 記録を SessionEnd 主に戻す（ユーザー決定）。理由: 起動時に注入される `current.md` が 1 セッション古いことをユーザーが許容しない。他プロジェクトを覗いたときの誤認にもなる。**ただし CLI の有無で主経路を選ぶ**——CLI があれば SessionEnd で録音係を起動し、無ければ終了印だけ置いて次の起動が拾う。捨てた案: CLI 必須で単純に保つ（2026-08-30 が「環境制約でヘッドレスが使えない利用者が一切記録できない」を理由に捨てた形に戻る。ユーザー自身が該当する環境を持っている）、現状維持（`current.md` の鮮度がユーザーの要件を満たさない）。2026-08-30 が「CLI があるときだけ従来経路」を「経路が 2 本になり試験も 2 本分」で却下したのに対し、**今回は SessionStart 側の経路がどのみち残る**（SessionEnd が発火しない終了経路のため）ので、2 本目は新設ではなく既存の使い回しになる。対立した要件: 実装の単純さ vs 全環境で記録が残ること——後者を採った。
- 2026-09-02 SessionEnd hook は「終了印を置き、録音係を 1 つ起動し、ログを 1 行書いて exit」だけにする。実測 20ms で、1.5 秒の上限から 2 桁の余裕がある。録音係は親を explorer.exe に付け替えて産み、開けなければ `sihost.exe` → `RuntimeBroker.exe` の順に試し、全部失敗したら素の DETACHED に落ちる。付け替えのコストは素の起動比 +8ms（13.7〜18.3ms vs 4.5〜10.0ms）で、分岐を持つ価値がないほど小さい。**異常時だけ別の起動方式に切り替える設計にしないのは、その分岐が普段動かないぶん腐るため。**ウイルス対策が python.exe の起動をスキャンして数百 ms〜数秒かかるのは現実に起きるので、上限を超える経路は実在する。
- 2026-09-02 録音係（ヘッドレス）はファイルを書かない。`claude -p <ジョブ本文> --allowedTools Read` で走らせ、応答を Python が inbox へ置く。理由: 書き込みをすべて `relay_apply` に集める原則（2026-07-22）をヘッドレス経路でも保つ。ジョブ本文はサブエージェント経路と共有し、「Write ツールで書け」を「応答本文に出せ」と読み替える前置きだけを足す。**本文を 2 つに分けないのは、60 行のプロンプトの複製が必ず食い違い、その食い違いが「片方の経路でしか出ない節」として現れるため。**ジョブの順序（記録 → apply → 現在地 → apply → 統合）は Python が制御する。モデルに「3 つを順に」を守らせない。
- 2026-09-02 録音係が走っている間、`~/.claude/relay/running/<プロジェクト>/<sid>` に印を置く。SessionStart はその sid を候補から外し、注入文に「記録中で `current.md` はその分を含まない」と明示する。理由: 検出は日記だけを見るので、録音係が書き終わるまでの 2〜4 分、そのセッションは「誰も触っていない」ように見える。印が無いと次の起動が二重に記録する。**古い印（30 分）は録音係の死亡とみなすが、削除はしない**——消すのは録音係の仕事で、外から片付けると「遅いだけの録音係」の印を消す競合になる。捨てた案: 2026-08-30 に廃止した台帳を戻す（未記録かどうかを日記から読む設計に反する。この印は「記録済み」ではなく「作業中」を表すので層が違う）。
- 2026-09-02 録音係はプロジェクト単位で直列化する（`running/<プロジェクト>/.lock` を `O_EXCL` で取る）。理由: 2 本が並行して `current.md` を書くと後勝ちになり、2026-08-01 が `.status` サイドカーで解いた競合が再発する。順序を付ければ 2 本目は 1 本目の日記が入った状態を読むので、後勝ちが正しい順序になる。**持ち主の生死は pid で判定せず、ロックの mtime だけで決める**（30 分）。pid は再利用されるので、再利用された pid は「生きている」と読めてしまい、そのプロジェクトが永久に詰まる。pid と時刻はファイルに書くがログ用。待ちの上限は 10 分で、超えたら印を残して終わる（次の起動が拾う）——待ちは生きたプロセスを消費するので、連続して閉じたセッションぶん積み上がってはいけない。
- 2026-09-02 UserPromptSubmit hook を 1 つ足し、**起動時に記録中だったセッションの記録が終わったときだけ**、1 セッションに最大 1 回、その旨を 1 行出す。**これは「relay はセッション中に口を出さない」原則の唯一の例外。**理由: 起動時に「`current.md` はまだ古い」と断った以上、それが終わったことを伝えないと、読み手は正しくなったページを疑い続けるか、断り書きを忘れて古いものを信じる。判定は watch ファイルと `running/` の stat 2 回で、Stop hook のような全走査はしない（あちらはウォーム 70〜250ms で、それが退役の理由の一つだった）。既読印は `~/.claude/relay/notified/<プロジェクト>/<sid>.watch` を消すことで表す。
